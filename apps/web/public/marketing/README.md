# Marketing assets

Flyers, hero images, and social/og:image graphics — promotional pieces, not
in-app UI icons (those stay simple/minimal per the root CLAUDE.md's UI
Philosophy). Anything dropped here is served statically at `/marketing/<file>`.
